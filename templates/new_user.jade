extends base

block titulo
    |Nuevo Usuario


block body_content
    | {% if user.is_authenticated %}
    div(class="col-xs-10 col-xs-offset-1 col-sm-6 col-sm-offset-3 col-md-6 col-md-offset-3 col-lg-6 col-lg-offset-3 resetCol")
        form(action="",method="post",class="newCategoryForm",enctype="multipart/form-data")|{% csrf_token %}
            | {{form.as_p}}
            button(type="submit",class="btn btn-success btn-block") Aceptar
    | {% endif %}